# 炼金炉 v2.0.1

## 本次更新

- Windows 新增正式 `炼金炉.exe` 启动器，双击即可启动本地服务并打开浏览器，不再需要手动运行 BAT。
- Windows 包内置 Python 3.12 x64 运行时，保持免安装、离线启动方式。
- Windows 启动失败信息写入 `data/launcher.log`，便于排查本地服务问题。
- 更新首页三种模式卡片素材，修复卡片图片白色背景和显示适配问题。
- 优化模式卡片点击后的项目筛选与说明区域，避免说明条遮挡内容。
- macOS 继续保留 `.app` 启动方式，并同步当前最新源码、页面和素材。

## 使用提示

- Windows：解压完整压缩包后双击 `炼金炉.exe`。
- macOS：解压完整压缩包后双击 `炼金炉.app`。
- Release 包不包含用户本地项目、API Key 和个人配置；升级前请保留原有 `data/` 目录。
- 应用启动后访问 `http://127.0.0.1:8765`。

## 已知限制

- `.xls` 文件需要先另存为 `.xlsx`。
- PDF 和 CHM 的深度提取依赖系统工具 `pdftotext`、`extract_chmLib`。
